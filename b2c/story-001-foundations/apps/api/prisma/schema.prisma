generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url = env("DATABASE_URL")
}

// Minimal model to ensure migration works
model Health {
  id Int @id @default(autoincrement())
  createdAt DateTime @default(now())
}
